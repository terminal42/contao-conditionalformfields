<fieldset{% if this.class|default %} class="{{ this.class|default }}"{% endif %}{% if this.isConditionalFormField|default %} data-cff-condition="{{ this.conditionalFormFieldCondition|e('html_attr') }}"{% endif %}>
    {% if this.label %}
        <legend>{{ this.label|insert_tag_raw }}</legend>
    {% endif %}
